import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.andy.medicab',
  appName: 'Medicab',
  webDir: 'dist/medicab',
  bundledWebRuntime: false
};

export default config;
